#include<stdio.h>
#include<string.h>
void main()
{
char a[20];
printf("enter the sentance");
gets("a");
int len=strlen(a);
int count=0;
for(i=0;i<len;i++)
{
if(a[i]==' ')
count=count+1;
}
printf("the no.of words is %d",count+1);
}
